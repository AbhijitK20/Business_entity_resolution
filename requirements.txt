# Amazon ML Entity Resolution Pipeline - Dependencies
lightgbm>=4.0.0
xgboost>=2.0.0
scikit-learn>=1.3.0
pandas>=2.0.0
numpy>=1.24.0
optuna>=3.0.0
shap>=0.42.0
jellyfish>=0.11.0
rapidfuzz>=3.0.0

# Blocking. datasketch powers the MinHash/LSH leg — a primary recall path.
# It was imported but never declared, so a clean install silently fell back to
# RapidFuzz and lost recall. indic-transliteration powers cross-script
# normalization (22.7% of India pairs are cross-script).
datasketch>=1.6
indic-transliteration>=1.5
